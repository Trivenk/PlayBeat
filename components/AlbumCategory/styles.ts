import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        margin: 10,
    },
    title:{
        margin:10 ,
        color: 'white',
        fontSize:24,
        fontWeight: 'bold'
    }
})

export default styles;